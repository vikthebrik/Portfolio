---
title: Autonomous Mapping Drone
slug: autonomous-mapping-drone
category: drone
summary: A quadrotor that builds dense 3D maps of GPS-denied interiors in real time using onboard SLAM.
tags: [slam, computer-vision, ros2, embedded]
related: [thesis-slam, realtime-cv-pipeline]
year: 2025
featured: true
# External media — CDN / stream host, never committed to the repo.
cover: https://cdn.example.com/portfolio/drone/mapping-cover.webp
video: https://stream.example.com/portfolio/drone-flythrough
links:
  repo: https://github.com/yourname/autonomous-mapping-drone
  demo: https://example.com/demos/mapping
---

## Overview

Write the case study here in MDX — prose plus any React components you register
in `mdx-components.tsx`. This body is what loads into the content panel when the
graph collapses to the `detail` state.

This file alone adds:

- one **project node** (`autonomous-mapping-drone`) in the `drone` cluster,
- a **membership** edge to the `drone` hub,
- two **related** edges to `thesis-slam` and `realtime-cv-pipeline`
  (which place this node near the `research` cluster — exactly the cross-pillar
  density you want),
- faint **tag** edges to anything else sharing `slam`, `computer-vision`, etc.

No graph config was touched. That is the whole point.
