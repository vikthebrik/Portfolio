# dependencies
/node_modules

# next.js
/.next
/out

# velite (content layer output — derived, never committed)
/.velite
/public/static

# env (contains secrets — never commit)
.env
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
npm-debug.log*
