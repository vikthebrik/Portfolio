# dependencies
/node_modules

# next.js
/.next
/out

# velite (content layer output — derived, never committed)
/.velite
/public/static

# env (contains secrets — never commit)
.env
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
npm-debug.log*

# playwright mcp verification artifacts (root-level screenshots only — inline
# diagram PNGs beside MDX in content/ must stay tracked, see CLAUDE.md media strategy)
.playwright-mcp/
/*.png

# raw media staged for Blob upload — never committed (CLAUDE.md media strategy)
/*.zip
/*.pdf

# playwright test output
/test-results/
/playwright-report/
